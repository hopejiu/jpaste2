# 2026-07-09

## jPaste 前后端架构深化评审（improve-codebase-architecture skill）
生成 HTML 报告到 `%TEMP%/architecture-review-2026-07-09.html`（已打开）。

发现 10 个候选（前后端 + 冗余/重复/死代码）：
- B1 [Strong] Repository 图片/缩略图路径查询 `SELECT image_path, thumb_path` 重复 3 处（cleanup/collect_image_paths/get_image_storage_bytes）→ 抽 collect_path_pairs helper。
- B2 [Strong] `get_copy_count` 死代码（`#[allow(dead_code)]` 无生产调用）；copy_count 读取 SQL 重复 3 处 → 删死代码 + 抽 read_copy_count。
- B3 [Strong] 前端相对时间格式化重复：`lib/format.ts::formatTime` vs `timestamp-view.tsx::buildResult`（62-66）手写一份 → 复用 relativeTime()。
- B4 [Worth] `settings_changed_except_hotkey` 手工罗列 14 字段，漏加静默失效 → 改 serde 结构比较（排除 hotkey）。
- B5 [Worth] `on_settings_change` 生产从未订阅（仅测试用）→ 死基础设施，考虑删除。
- B6 [Strong] `ClipboardPipeline` 浅模块 + lib.rs 上帝文件(822行)；process_with_pipeline(~240行) 堆满捕获→Toast 逻辑 → 抽 capture 模块可单测。
- B7 [Worth] Toast action 检测分散：model.rs detect_actions 与 lib.rs 内联 qrcode/folder 裁剪各管一半。
- B8 [Worth] 6 个 viewer 页面入口脚手架重复 → 抽 useViewerEntry hook，与后端 VIEWERS registry 对称。
- B9 [Speculative] 跨语言 action 检测重复（Rust detect_actions vs TS actions/*），ponytail 注释已承认，去重不现实，维持双端测试。
- B10 [Worth] 文档/实现漂移：`notify_opacity` 在设计文档/memory 记为已实现，但 settings.rs::Data 无此字段 → 核查是否走 action_config 或确为未持久化 bug。

Top 推荐：先 B1+B2+B3 安全去重，再 B4+B5，再 B6（核心），再 B8。对比 2026-06-08 评审：B1 自动收藏重复已修复，F1 MainPage 已拆分。

## 实现完成情况（用户"全做" + "其他按架构html来"）
- B10（notify_opacity）上一轮已处理：纯 phantom drift（代码无此字段），仅更新 `docs/技术方案.md` 删除 2 处引用，不实现透明度。
- B1+B2：repository.rs 抽 `collect_path_pairs`/`flatten_paths` + 删死代码 `get_copy_count` 改 `read_copy_count`。
- B3：`lib/format.ts` 加 `relativeTime()`，timestamp-view 复用；删前端重复相对时间逻辑。
- B4+B5：settings.rs 删 `settings_changed_except_hotkey` + `on_settings_change` 死基础设施（test 同步删）。
- B6+B7：新建 `src-tauri/src/capture.rs`，从 lib.rs 抽 `build_toast` + `build_toast_actions`；lib.rs 删 `process_with_pipeline`(~190行)，worker 调 `capture::build_toast`。
- B8：新建 `src/hooks/use-viewer-entry.ts`，6 个 viewer 页面（calc/decoder/timestamp/ws/curl/json）全部改用，删重复脚手架 + 合并 fetch/logError。
- B9：保持现状（报告本身标 Speculative / 维持双端测试）。
- 验证：`cargo check` 通过（0 error/0 warning），`tsc --noEmit` 通过（exit 0）。修复：capture.rs 的 `ClipboardPipeline` 路径应为 `clipboard::pipeline::ClipboardPipeline`；curl-view 缺 `api` 导入；decoder/timestamp 删多余 `logError` 导入。

## 快速启动合并到工具箱 + 快捷键修复（用户两项需求）
- 需求2（快捷键未生效）根因：dispatch handler 用 `shortcut.to_string()`（canonical 如 "Alt+KeyV"）匹配 `launch_hotkey_map`，但 map 键是用户键入串（"Alt+V"）且 `save_launch_targets` 从未更新 map。修复：(a) lib.rs 初始化 map 时按 `Shortcut::to_string()` 规范化键；(b) quicklaunch.rs `save_launch_targets` 保存后重建 `launch_hotkey_map`（lock_state 写入 AppState）。`cargo check` 通过。
- 需求1（合并）：用户纠偏——不是把各启动目标摊成 toolbox 卡片，而是「整个快速启动模块 = toolbox 里像 JSON 一样的**一张**小卡片」，点击打开**新窗口**，窗口里是快速启动的**完整内容**（列表+添加/编辑/启动，配置在窗口内用 LaunchModal 完成）。
- 实现：toolbox 页 TOOLS 数组首张卡为「快速启动」(rocket)，点击调 `api.openQuicklaunch()`；其余为原 6 个 viewer 卡片。还原已删的 `src/routes/quicklaunch/index.tsx` + `launch-modal.tsx`（git checkout），QuickLaunchPage 改为独立窗口版（去掉侧边栏导航，标题栏含拖拽区 + 关闭按钮 `getCurrentWindow().close()`），列表/添加/LaunchModal 不变。
- Rust：删除 `open_launch_config(id)`，新增 `open_quicklaunch()`（复用 build_viewer_window，单窗口 label="quicklaunch"，480×620，路由 /quicklaunch，重开则 set_focus）。lib.rs 注册同步改。invoke.ts `openQuicklaunch()`。
- 删除 /launch-config 页面及 `.lc-*` + `.lm-btn-danger` CSS（不再需要）。
- 验证：`cargo check` 0 error，`tsc --noEmit` exit 0。侧边栏仅 剪贴板/工具箱。
