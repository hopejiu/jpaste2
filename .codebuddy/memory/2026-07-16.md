# 2026-07-16

## 修复：快速启动网页窗口最小化后用快捷键无法呼出
- 根因：quicklaunch.rs `launch_web` 在窗口已存在且 `is_minimized()` 为 true 时，只调用 `show_focus_window`（仅 `show()` + `set_focus()`）。Windows 上 `show()` 不会恢复最小化窗口，导致窗口停在任务栏无法呼出。
- 修复：在 minimized 分支先 `window.unminimize()` 再 `show_focus_window`。改动仅限 src-tauri/src/command/quicklaunch.rs。
- 注意：cargo 因 D: 盘 reparse point 被 PowerShell 视为不受信任挂载点而无法运行，未能本地编译验证；但 `unminimize()` 是 Tauri v2 `WebviewWindow`（deref 到 `Window`）的合法方法。

## 修复：share-panel 拖拽报错 `plugin:event|listen not allowed by ACL`
- 根因：`capabilities/default.json` 的 windows 只为 `["main","toast-0","*-viewer-*"]` 授权，share 面板窗口 label 是 `share-panel`，不匹配任何模式，因此拿不到 `core:event:allow-listen`，`onDragDropEvent` 监听被 ACL 拒绝。
- 修复：在 windows 数组加入 `"share-panel"`。改动限 src-tauri/capabilities/default.json。

## 修复：HTTP 共享下载由纯 JS 改为直链
- 背景：下载原用 `downloadFile()`（fetch+blob+anchor 触发），分别在 Rust `render_list_page` 服务端初始渲染和 `client.js` 的 `renderItem` 前端刷新渲染两处。
- 修复：两处下载按钮改为 `<a class="btn" href="/d/{id}">`，依赖 download handler 已返回的 `Content-Disposition: attachment` 触发原生下载；删除 `client.js` 中已变死代码的 `downloadFile()`；给 `.btn` 加 `text-decoration:none`（anchor 默认下划线）。改动限 share_server.rs / share-client/client.js / share-client/styles.css。
- 注：原注释称 Android Chrome 忽略原生 `<a download>`，但普通 `<a>`（无 download 属性，靠 Content-Disposition）在移动端可靠触发下载。

## 功能：解码工具新增 转义/解转义 模式（第 4 种）
- action.ts：viewer action 检测新增"转义字符串"——至少 2 个转义序列（`/\\(["'ntrb\/\\])/g`），避免误判单处 `\"`。
- index.tsx：新增 `DecodeMode='escape'`，加入 MODE_ORDER、encode/decode 分发、detectMode、loading 的 looksEncoded。实现 `encodeEscape`/`decodeEscape`（逐字符/逐字符扫描，`\\n` 正确解为反斜杠+n 而非换行），`isLikelyEscaped` 检测器。Tab 标签显示 "Escape"。无 lint 错误。
- 检测顺序调整：action.ts 与 detectMode 均统一为 url → unicode → escape → base64，base64 最易误触发故放最后兜底。

## 优化：HTTP 共享下载体验（浏览器直接下载 + 文件名可点）
- 给 `download_handler` 响应加 `X-Content-Type-Options: nosniff`，防止浏览器对 .html/.svg/.txt 等做 MIME 嗅探并当成网页渲染（与 `Content-Disposition: attachment` 共同强制下载）。
- 共享列表里的文件名从 `<span>` 改为 `<a href="/d/{id}">` 下载链接（Rust `render_list_page` 与 JS `renderItem` 两处一致），点击文件名即可下载；`.name` CSS 加 `text-decoration:none` + hover 下划线反馈。
- 未改动：文本条目仍是“复制”（inline 展示便于复制），打开 `/t/:id` 直接看文本，不强制下载。

## 优化：curl 查看器 (HTTP 调试) UI（features/curl/index.tsx + styles/viewers.css）
- HTTP 方法下拉框按动词着色（Postman/Insomnia 约定）：GET 绿/POST 琥珀/PUT 蓝/PATCH 紫/DELETE 红/HEAD·OPTIONS 灰。用 `data-method={method}` 属性 + CSS `[data-method=...]` 选择器实现。
- 响应状态码改为药丸徽章：`.curl-status` 加圆角 pill + 半透明底色，ok/redirect/error 各自配色，“未发送”用中性灰底。
- 修复 `.curl-url-row` 双重内边距（自身 12px + `.curl-card-body` 12px = 24px）→ 删除 url-row 自身 padding。
- 发送按钮与 URL 输入框贴太紧 → `.curl-url-row .viewer-btn { margin-left: var(--space-2) }`。
- 修复请求卡片 desc 短 URL 也显示 `...` 的问题：改为 `fullUrl.length > 60` 时才加省略号。
